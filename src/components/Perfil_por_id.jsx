const Perfil_por_id = ({ profile_id }) => {
    // preparando o props com o profile_id para usar dps

    return (
        <section>
            <h1>PEGAR PERFIL DE OUTRA PESSOA PELO ID DA URL AQUI: <br /> {profile_id}</h1>
        </section>
    )
}

export default Perfil_por_id;